{
  "mcpServers": {
    "supabase-rflex": {
      "command": "npx",
      "args": [
        "-y",
        "@supabase/mcp-server",
        "--read-only"
      ],
      "env": {
        "SUPABASE_PROJECT_REF": "xenapowdtfhdwcfthfrn"
      },
      "description": "Conector Supabase para inspeção de schema, advisors e diagnóstico em modo seguro (read-only) do App Reflex 02."
    },
    "github-rflex": {
      "command": "npx",
      "args": [
        "-y",
        "@modelcontextprotocol/server-github"
      ],
      "env": {
        "GITHUB_PERSONAL_ACCESS_TOKEN": "${GITHUB_TOKEN}"
      },
      "description": "Conector GitHub oficial para PRs, observação de Actions e Code Security."
    }
  }
}
